// Banner Container
import LayoutBannerPresenter from "./LayoutBanner.presenter";

const LayoutBannerContainer = (props) => {
    return (
        <>
            <LayoutBannerPresenter />
        </>
    );
};

export default LayoutBannerContainer;
